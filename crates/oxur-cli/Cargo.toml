[package]
name = "oxur-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "CLI infrastructure and unified command-line tool for Oxur"

# Binary configuration - the unified `oxur` tool
[[bin]]
name = "oxur"
path = "src/main.rs"

# Library configuration - common utilities for all Oxur CLIs
[lib]
name = "oxur_cli"
path = "src/lib.rs"

[dependencies]
# For library utilities
tabled = "0.17"
serde = { workspace = true }
toml = { workspace = true }
anyhow = { workspace = true }
colored = { workspace = true }


# For binary (when building the oxur command)
oxur-lang = { path = "../oxur-lang", optional = true }
oxur-comp = { path = "../oxur-comp", optional = true }
oxur-repl = { path = "../oxur-repl", optional = true }
clap = { workspace = true, optional = true }

[features]
default = ["binary"]
# Feature for the binary (not needed by library users)
binary = ["clap", "oxur-lang", "oxur-comp", "oxur-repl"]

[dev-dependencies]
tempfile.workspace = true
